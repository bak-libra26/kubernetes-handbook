# Headless Service: clusterIP를 None으로 설정
# DNS 조회 시 개별 Pod IP가 직접 반환됨
# 주의: StatefulSet과 함께 쓸 때 각 Pod에 고유 DNS 부여 가능
apiVersion: v1
kind: Service
metadata:
  name: headless-svc
spec:
  clusterIP: None
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
