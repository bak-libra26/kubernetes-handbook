# NodePort: 노드 IP + 포트로 외부 접근 허용
# 주의: nodePort 범위는 30000-32767만 가능
apiVersion: v1
kind: Service
metadata:
  name: nodeport-svc
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      # 접근: http://<노드IP>:30080
